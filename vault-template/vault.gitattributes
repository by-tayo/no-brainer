# Normalise line endings so the repo stays clean across machines
# and git stops warning on every commit.
* text=auto eol=lf

# Binary assets — never touch these
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.pdf binary
*.zip binary
*.canvas text eol=lf
